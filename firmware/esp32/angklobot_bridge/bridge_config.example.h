#pragma once
// Copy to bridge_config.h (ignored by Git). Use a trusted isolated local Wi-Fi.
#define WIFI_SSID "YOUR_LOCAL_WIFI"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
#define BRIDGE_TOKEN "CHANGE_ME"
